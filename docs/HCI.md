
## P4 HCI (Preview/Authorization Flow)

Preview → shows Before/After diff, privilege, risk, benefit, rollback, reboot → explicit [ Approve ] → Authorization [ Authenticate ] (native Polkit) → Backup ✓ → Apply → Verify → Completed [ Rollback ]. P4 CLI `transaction preview` shows same structure for future GUI.

