# Roadmap - Polaris

P1 Arch - done scaffold, docs, no host modify
P2 Read-Only - real providers (proc/sys/dbus), health, JSON, CLI
P3 Perf - baseline, benchmark quick/normal/deep, bottleneck confidence
P4 Safety - tx, backup, dry-run, approval, Polkit helper allowlist
P5 L1 - user kwinrc, autostart
P6 L2 - systemd, fstab swap, sysctl with approval
P7 L3 - nvidia 470xx, GRUB with explicit + reboot
P8 Hardening - audit, fuzz, systemd sandbox
P9 RPM - polaris.spec
P10 GUI - Qt6 Widgets/Quick as API client only

P4 Safety + Transaction + Backup/Rollback + Polkit + Audit + Dry-Run - **COMPLETE 2026-08-31**, infrastructure ready, no real optimizations, test fixtures only.


P8 Read-Only Discovery (Post-P7) - **COMPLETE 2026-09-01 00:50**, re-ran baseline (userspace 8.515s vs 54.106s -84%, failed 0 vs 1, NVRM 1 vs 490, swap 0 vs 1.6GB), ranked 7 candidates, identified TX-P8-AKONADI-DISABLE-PREVIEW as highest-value (1.3GB, R2, no reboot, needs user confirmation), state PREVIEWED -> APPROVAL_REQUIRED, awaiting explicit approval for P9.

P9 Fine-Grained Optimization & Validation (Post-P7) - **COMPLETE 2026-09-01 00:55**, fresh baseline 8.515s userspace (vs 54.106s -84%), re-ranked 6 candidates, all remaining have negligible benefit (0s boot or tiny 5-10M) or require user confirmation with low confidence, Akonadi rejected (user uses KMail), so **NO_ACTION_RECOMMENDED** - no transaction previewed, read-only, no host modification.

P10 Planning - Post-Change Measurement & Regression Detection (P10_PLAN.md) - **PLANNED 2026-09-01 01:00**, gap analysis 10 areas, ranked 10 tasks, selected #1 Post-Change Measurement (Comparison + RegressionEngine) as highest-value, with implementation plan, tests, acceptance criteria - **STOP, awaiting approval before implementation**.

P11 Post-Change Measurement & Regression Detection - **COMPLETE 2026-09-01 01:20**, implemented `core/domain/Comparison.h` + `core/engines/comparison/ComparisonEngine` (pure, deterministic, 6 metrics, thresholds boot +10%, mem 1GiB, thermal 15C, failed new, `expected vs observed`, `unavailable` handling), extended `Transaction` with `beforeBaseline`/`afterBaseline`/`comparison` (backward compatible), CLI `transaction show --json` + `compare` expose comparison, P7 fixture and 15-category tests (normal, unchanged, regression, unavailable, zero-before, boundary, etc.) 100% pass, no host mutation, docs updated.

P12 Transaction/State-Machine Hardening (Stale-Preview + Idempotency) - **COMPLETE 2026-09-01 03:30**, extended `Transaction` with `beforeHash/approvedBeforeHash`, `beforeUnitHash/approvedUnitHash`, `kernelVersion/approvedKernelVersion`, `packageStateHash/approvedPackageStateHash`, `approvedTarget/approvedOperation`, `preconditions/approvedPreconditions`, `idempotencyKey/appliedAt/completedAt/validationResult` (backward compatible), added `TransactionValidator` (pure, fail-closed, stale checks for target/operation/beforeHash/unitHash/kernel/package/precondition/TOCTOU), `TransactionStore` (duplicate deterministic, idempotent apply/verify, backup boundary APPROVAL→VALIDATION→BACKUP→FINAL VALIDATION→APPLY, audit with expected/observed), hardened `StateMachine` (`PREVIEWED/APPROVAL_REQUIRED/APPROVED → FAILED` for stale, `isTerminal` helper, illegal transitions still rejected), `AuditLog` fsync per event, 13/13 tests 0.11s (existing 9 + 4 P12 suites: p12_stale 10 cats + TOCTOU + final, p12_idempotency 5 cats, p12_statemachine 20 transitions, p12_transaction_model 3 cats) - fixtures `/tmp/polaris-test-root` only, no host mutation.

P13 User Workflow / Profile Engine - **COMPLETE 2026-09-01 04:30**, implemented `UserProfile` (`TriState` unknown/yes/no for 8 fields + extra, deterministic JSON, `==`, `isDefaultUnknown`), `ProfileStore` (`~/.local/state/polaris/profile.json` atomic temp+fsync+rename 0600, no symlink, canonical validation, `FileSafety` allowlist, missing→unknown no auto-create, malformed→throw + audit), `ProfileService` (explicit `updateField` `yes/no/unknown`, no inference, idempotent `already` audit, `profile.updated` audit with `field`/`previous`/`new`), `ProfileAdvisor` (`BLOCKED_BY_USER_WORKFLOW` for `yes`, `REQUIRES_USER_CONFIRMATION` for `unknown`, `ALLOWED_FOR_ANALYSIS` for explicit `no` with `RECOMMEND→PREVIEW→APPROVAL→BACKUP→APPLY→VERIFY` still required, `whatWillNotChange`/`confirmationRequired` explainability), CLI `polaris_p4 profile show|set` (`--json`, auditable, not authorization), 17/17 tests 0.13s (existing 13 + 4 P13 suites: profile_model 6 cats, profile_store 6 cats, profile_service 6 cats, profile_advisor 12 cats) - fixtures `/tmp/polaris-test-root` only, real profile not touched.

P14 Expanded Security & IPC / Helper Architecture - **COMPLETE 2026-09-01 05:30**, implemented `IpcProtocol` (`PROTOCOL_VERSION=1`, `MAX_REQUEST_SIZE=64KB`, `MAX_ARG_COUNT=16`, `MAX_ARG_SIZE=4096`, `validate`/`validateRaw` with NUL/shell/traversal/oversized/unknown/password checks, `serialize`/`parse` deterministic, `allowedOperations` `ping`/`info` only, no privileged mutation), `IpcAuth` (`SO_PEERCRED` `getPeerCred`/`isAuthorized` same-user, `containsSpoofedCred`, fail-closed on unavailable), `IpcServer`/`IpcClient` (Unix socket `/run/polaris/helper.sock` defined but never created, tests use `/tmp/polaris-test-root/p14/helper.sock` with `validateSocketPath`/`checkParentSecurity` `0700`/`0600` not world-writable, no symlink, `isStaleSocket` `ECONNREFUSED`, `poll` 5s timeout, one request per connection, `FD_CLOEXEC`, `handleRequest` auth→spoof→validate→allowlist→`ping`/`info` with audit `ipc.*`), `TransactionLock` (`flock` exclusive non-blocking `lock.acquire`/`rejected`/`release`, `0600`, parent symlink/world-writable check, concurrent test), `RecoveryDetector` (`detect` scans `BACKUP_CREATED/APPLYING/APPLIED/VERIFYING` → `incomplete` with `suggested=FAILED` fail-closed, never auto-apply, `backupExists` check, audit `recovery.detected`), audit `ipc.*`/`lock.*`/`recovery.*` with `TX-TEST-IPC` prefix for test log, `fsync` preserved, authenticated≠approved≠applied distinction, no bypass of `StateMachine`/`TransactionValidator` (tests verified), 24/24 tests 0.57s (existing 17 + 7 P14 suites: ipc_protocol 12 cats, ipc_auth 5 cats, socket_security 5 cats, ipc_server 4 cats, lock 5 cats, ipc_security 12 cats, recovery 4 cats) - fixtures `/tmp/polaris-test-root/p14` only, real helper socket not created.

P15 Test / CI / Fixture Expansion - **COMPLETE 2026-09-01 06:30**, added `TransactionValidator` fail-closed fix for `UNAVAILABLE` (`kernel`/`package`/`beforeHash` empty→`unverifiable`), 5 new table-driven deterministic isolated test suites: `test_p15_lifecycle` (valid 16 + rejected 12 `StateMachine` table, stale→FAILED, unverifiable→FAILED), `test_p15_stale_matrix` (7 fields ×3 states 19 cases `UNCHANGED` accepted `CHANGED`/`UNAVAILABLE` rejected multi-field deterministic), `test_p15_toctou_idempotency` (TOCTOU between gates no mutation backup preserved `FAILED`, symlink TOCTOU, idempotency `create`/`approve`/`apply`/`verify` + reload via `exists`), `test_p15_lock_recovery` (exclusive `flock` table 2/4/3 threads, `FD_CLOEXEC`, stale parent, recovery table 10 states, scan `BACKUP_CREATED`/`APPLYING` detected, `COMPLETED` not, never auto-apply, rollback backup stable not overwritten), `test_p15_regression_audit` (regression 16 thresholds `==10%` not `just above` regression, `unavailable` not, multi, `observedBenefit` positive/zero/negative, `FileSafety` 12, `IpcProtocol` 6, `Audit` chain, fixture isolation `p15_iso1` vs `p15_iso2`, real profile not mutated), plus `.github/workflows/ci.yml` minimal (`cmake --fresh`, `cmake --build`, `ctest`, `test ! -f /run/polaris/*`), 29/29 tests 0.70s (existing 24 + 5 P15) - fixtures `/tmp/polaris-test-root/p15` only, no host mutation, `FileSafety`/`IpcProtocol`/`AuditLog` still fail-closed, `BackupEngine` no-overwrite, `RecoveryDetector` detection-only.

P16 Explainability - **COMPLETE 2026-09-01 07:30**, implemented `Explanation` (22 fields deterministic sorted JSON, `toHuman` verbose redacted), `ExplanationEngine` (`explainCandidate` `WHY NOW`/`WHAT WILL CHANGE`/`WHAT WILL NOT CHANGE`/`rejectionConditions` with `ProfileAdvisor` `BLOCKED`/`REQUIRES`/`ALLOWED` + `Comparison` `expected`/`observed`/`verdict`/`hasRegression`, `explainTransaction` lifecycle `PREVIEWED`→`FAILED` with `expected`/`observed`/`backup`/`rollback`/`authorization` distinction, never `password`/`sh -c`), CLI `polaris_p4 explain <candidate> [--json] [--verbose]` and `transaction explain <id> [--json] [--verbose]` (human `WHY NOW`/`WHAT WILL CHANGE`/`WHAT WILL NOT CHANGE`/`EVIDENCE`/`EXPECTED BENEFIT`/`CONFIDENCE`/`RISK`/`REVERSIBILITY`/`REBOOT`/`AUTHORIZATION`/`REJECTION CONDITIONS`/`ROLLBACK`/`OBSERVED RESULT`/`VERDICT`, JSON deterministic, audit `explanation.generated`), 33/33 tests 0.70s (existing 29 + 4 P16 suites: explanation_model 6 cats, explain_candidate 7 cats, explain_transaction 8 cats, verbose_redaction 8 cats) - fixtures `/tmp/polaris-test-root/p16` only, no host mutation, `FileSafety`/`ReadOnlyGuard`/`StateMachine`/`TransactionValidator`/`AuditLog` still fail-closed, `IPC` `ping`/`info` only.

P17 Campaign 2 / Evidence-Backed Real-Host Optimization - **COMPLETED (NO_ACTION_RECOMMENDED) 2026-09-01 07:45**, read-only discovery 03:18 `systemd-analyze` 8.515s `graphical.target 8.514s` `critical-chain` `plasmalogin 7.301s` not `plocate` 21s, `free` 5.8Gi `zram` 0B `sensors` 58C, `akonadi` 14 agents 1302M `ProfileAdvisor` `BLOCKED` (handoff) / `REQUIRES` (file unknown), `bluetooth` `enabled` `active` 2 paired `TSCO-TS2343` `avahi` for `kdeconnect` `cups` socket, `plocate` `static` `21.111s` not in `critical-chain` `Nice 19`, `dnf-makecache` `timer` 1h 24min not in `blame` top, `explain` `bluetooth-disable` `REQUIRES_USER_CONFIRMATION` `WHY NOW` 2 paired, scored 7 candidates all `0s` boot-critical or `5-10M` tiny `REQUIRES`/`REJECTED` → `NO_ACTION_RECOMMENDED`, exactly ONE considered `bluetooth-disable` but negligible, no `PREVIEWED` transaction created, stop at `RECOMMEND` boundary, preserve invariants.

P18 Final Benchmark / ROI / Stability Report - **COMPLETED 2026-09-01 08:00**, final host validation 03:25 `systemd-analyze` 8.515s `critical-chain` `plasmalogin` `free` 5.6Gi `zram` 0B `lspci` `CLAIMED` `driver=nvidia` `modinfo` `470.256.02` `nvidia-smi` `470.256.02` `glxinfo` `Mesa Intel` `PRIME` `NVIDIA GeForce MX130` `akonadictl` `running` 14 agents `mssql` `disabled` `fstab` 3 entries `stat` `2026-08-31 21:19`, `Comparison` `P3` `54.106s`→`8.515s` `-84%` not `+10%` `available` `4.2→5.6` `+1.4GB` not `<-1GB` `thermal` `67→60` not `+15C` `nvidia` `0→1` not `failed` `zram` `0B`, `Transaction ROI` `TX-P6` `mssql` `713M` `0` `TX-P7` `nvidia` `470.256.02` `PRIME` `TX-P5` `NO_OP`, `Campaign 2` `NO_ACTION` `akonadi` `REJECTED` `bluetooth` `2 paired` `REQUIRES`, `Safety` `READ→...→AUDIT` demonstrated `stale`/`idempotency`/`flock`/`recovery`/`FileSafety`/`Ipc`/`Audit` `fsync`/`Profile`/`Explainability` `33/33`, no regression `drkonqi` `1` not `mssql`/`nvidia`, `PROJECT_COMPLETE_WITH_LIMITATIONS` `no P19 justified`.

Proposed Future Phases Ranked:
P11 - Regression Detection & Post-Change Measurement (COMPLETE)
P12 - Transaction/State-Machine Hardening (COMPLETE)
P13 - User Workflow/Profile Engine (COMPLETE)
P14 - Expanded Security & IPC/Helper (COMPLETE)
P15 - Test/CI/Fixture Expansion (COMPLETE)
P16 - Explainability & HCI (COMPLETE)
P17 - Campaign 2 / Evidence-Backed Real-Host Optimization (COMPLETE - NO_ACTION_RECOMMENDED)
P18 - Final Benchmark / ROI / Stability Report (COMPLETE)
